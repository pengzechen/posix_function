#include <stdio.h>

int main() {
    // 定义一个字符串
    char string[] = "Hello, World!";

    // 使用strlen()函数计算字符串的长度
    int length = strlen(string);

    // 打印计算出的长度
    printf("字符串的长度是：%d\n", length);

    return 0;
}
