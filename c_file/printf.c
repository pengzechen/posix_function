#include <stdio.h>

int main() {
    // 定义一些变量
    int number = 42;
    float pi = 3.14159;
    char letter = 'A';

    // 使用 printf 打印变量和文本
    printf("Hello, World! This is a demo of the printf function.\n");
    printf("The number is: %d\n", number);
    printf("The value of pi is approximately: %.2f\n", pi);
    printf("The letter is: %c\n", letter);

    // 程序正常退出
    return 0;
}
